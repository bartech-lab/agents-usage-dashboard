refresh_interval: 5m
server_port: 8777
providers:
  kimi:
    cookies:
      "kimi.com":
        "kimi-auth": "${KIMI_AUTH_TOKEN}"
  zai:
    api_key: "${ZAI_API_KEY}"
  codex:
    oauth:
      token_file: "${HOME}/.codex/auth.json"
  claude:
    cookies:
      "claude.ai":
        "sessionKey": "${CLAUDE_SESSION_KEY}"
